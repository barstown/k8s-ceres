# Grants tuppr (system-upgrade namespace) access to the Talos API for
# Talos and Kubernetes upgrades.
apiVersion: v1alpha1
kind: KubeTalosAPIAccessConfig
allowedRoles:
  - os:admin
allowedKubernetesNamespaces:
  - system-upgrade

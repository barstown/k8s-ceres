---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: flux-operator
spec:
  chartRef:
    kind: OCIRepository
    name: flux-operator
  interval: 1h
  values:
    serviceMonitor:
      create: true
    # web:
    #   enabled: true
    #   hostnames: ["flux.${SECRET_DOMAIN}"]
    #   httpRoute:
    #     enabled: true
    #     parentRefs:
    #       - name: envoy-internal
    #         namespace: network
    #         sectionName: https

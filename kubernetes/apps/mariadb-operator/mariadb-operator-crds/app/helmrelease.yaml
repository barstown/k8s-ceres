---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app mariadb-operator-crds
spec:
  interval: 1h
  chart:
    spec:
      chart: *app
      version: 26.3.0
      sourceRef:
        kind: HelmRepository
        name: mariadb-operator
      interval: 15m
  values: {}

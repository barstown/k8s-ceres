---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-media
  namespace: calibre
spec:
  storageClassName: ""
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 10Ti
  volumeName: calibre-media
